import styled from "styled-components"
import { TitleStyle } from "styles/globalStyles"

export const LatestPostsContainer = styled.section`
  padding-top: 1rem;
`

export const LatestPostTitle = styled.h2`
  ${TitleStyle}
  font-size: 1.25rem;
`
