import styled from "styled-components"

export const TagTitle = styled.h2`
  font-size: var(--font-size-h3);
  color: var(--gray-dark);
  margin-top: 0.5rem;
  margin-bottom: 1rem;
`

export const TagTitleDetails = styled.span`
  color: var(--gray-light);
`
